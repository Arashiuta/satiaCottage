# 通用前端项目习惯约束

> 适用范围：以 Vue 3、TypeScript、Vite、Vue Router、Pinia 和组件库为基础的Web 项目。  
> 本文从既有项目中反复出现的代码组织方式提炼而来，并将其中不稳定、偶发或明显不利于维护的写法统一为更可执行的约束。文中的“项目前缀”指当前产品、团队或组件库约定的短前缀，例如 `acme`；它不是某个具体项目名。

## 提炼出的稳定习惯画像

现有代码中反复出现、并被本文保留的核心习惯是：

- 使用清晰的分层目录区分页面、布局、公共组件、请求层、状态、工具和主题。
- 页面相关组件就近放置，弹窗、图表、预览器等独立交互单元主动拆分。
- 组件目录使用 `index.vue` 作为统一入口，降低引用路径和目录浏览成本。
- 使用 Vue 3 Composition API、`script setup` 和 TypeScript 编写组件。
- 用 Props、Emits、Slots 和 Expose 分别处理数据输入、动作上报、结构扩展和命令式能力。
- 用带项目前缀的通用组件统一按钮、弹窗、分页和列表框架等高频界面模式。
- 用 `Ref`、`List`、`Form`、`Visible` 等语义后缀表达变量用途，用动词表达函数动作。
- 组件样式默认局部化，使用 SCSS 嵌套组织结构，使用 CSS 变量实现主题切换。
- 路由 Meta 同时承载标题、菜单显示和权限信息，由路由配置派生导航结构。
- 将 Token、请求拦截、刷新队列、全局进度等横切能力集中在基础设施层。

本文没有保留格式混用、宽松类型、重复内联样式和资源未清理等偶发现象；这些内容已被归入禁止项或改进约束。

## 1. 约束级别与使用原则

本文使用以下关键词：

- **必须**：新代码必须遵守；不遵守时应在评审中说明理由。
- **应当**：默认采用；只有存在明确技术原因时才可例外。
- **可以**：可按业务复杂度选择，不要求所有模块机械套用。

约束的优先级如下：

1. 正确性、安全性和可访问性。
2. 当前仓库已经自动化执行的规则，例如 TypeScript、ESLint、Prettier、测试和构建检查。
3. 本文约束。
4. 当前文件已有且未被工具覆盖的局部风格。

执行时遵循四个总原则：

1. **按职责放置代码**：页面负责业务编排，组件负责可复用交互，基础设施负责横切能力。
2. **就近维护**：只服务于一个页面或模块的内容放在该页面或模块旁边。
3. **显式建立边界**：组件通过 Props、Emits、Slots 和 Expose 通信，不跨层修改内部状态。
4. **先保持简单，再按真实复用抽象**：不为尚未出现的场景创建过度通用的封装。

## 2. 推荐工程结构

```text
project-root/
├─ mock/                       # 本地模拟数据与模拟接口
├─ public/                     # 不经构建处理、按原路径发布的静态资源
├─ src/
│  ├─ api/                     # 按业务域组织的接口声明
│  │  ├─ types/                # 接口入参、出参、DTO 类型
│  │  └─ user.ts
│  ├─ assets/                  # 图片、图标等参与构建的资源
│  │  └─ <business-domain>/
│  ├─ components/
│  │  ├─ global/               # 全局注册、高频通用组件
│  │  └─ range/                # 跨页面复用但适用范围有限的组件
│  ├─ font/                    # 项目字体资源
│  ├─ layout/                  # 登录、主框架等页面骨架
│  │  └─ <layout-name>/
│  │     ├─ components/        # 只服务于该布局的组件
│  │     └─ index.vue
│  ├─ router/                  # 路由实例、路由表、守卫
│  ├─ service/                 # HTTP 实例、拦截器、请求级基础能力
│  ├─ store/                   # 全局状态
│  ├─ styles/                  # 主题变量、设计令牌、全局样式模块
│  ├─ utils/                   # 无业务或弱业务依赖的纯工具
│  ├─ views/                   # 路由页面
│  │  └─ <feature>/
│  │     ├─ components/        # 只服务于该功能的子组件
│  │     └─ index.vue
│  ├─ App.vue
│  ├─ main.ts
│  └─ style.css
├─ .env
├─ package.json
├─ tsconfig.json
└─ vite.config.ts
```

### 2.1 放置规则

| 内容 | 放置位置 | 判断标准 |
| --- | --- | --- |
| 路由页面 | `src/views/<feature>/index.vue` | 由路由直接加载，负责页面级业务编排 |
| 页面私有组件 | `src/views/<feature>/components/` | 只被当前功能使用 |
| 布局私有组件 | `src/layout/<layout>/components/` | 只参与某种页面骨架 |
| 全局基础组件 | `src/components/global/` | 高频使用、API 稳定，适合全局注册 |
| 范围型组件 | `src/components/range/` | 能跨页面复用，但包含特定领域或第三方能力 |
| 请求函数 | `src/api/` | 描述具体业务接口 |
| 请求/响应类型 | `src/api/types/` | 与后端契约直接相关 |
| HTTP 通用能力 | `src/service/` | 实例、拦截器、鉴权、重试、统一响应处理 |
| 全局状态 | `src/store/` | 多页面共享、需要响应式更新的状态 |
| 通用函数 | `src/utils/` | 与 Vue 组件无关，可独立测试 |
| 主题令牌 | `src/styles/` | 跨页面复用的 CSS 变量和主题定义 |
| 模拟数据 | `mock/` | 仅用于开发、演示或测试替身 |

### 2.2 就近维护与提升复用层级

组件首次出现时应放在离使用方最近的位置。只有满足以下任一条件，才提升到公共目录：

- 已被两个或以上互不依赖的功能使用；
- 已形成清晰、稳定、与具体页面无关的 Props/Emits API；
- 承载统一视觉或统一交互标准，例如按钮、弹窗、分页、表格框架；
- 封装独立的复杂第三方能力，例如图表、PDF、编辑器或上传器。

不得为了“以后可能复用”提前把业务组件放入全局目录。

## 3. 文件与目录命名

### 3.1 基础规则

- 目录名默认使用小写或 lowerCamelCase，例如 `themeDrawer`、`addDialog`。
- 一个组件占用一个目录，入口统一命名为 `index.vue`。
- 页面目录使用业务名，不使用 `page1`、`newPage`、`temp` 等无语义名称。
- TypeScript 工具文件使用 lowerCamelCase，例如 `formatTime.ts`、`downloadFile.ts`。
- 类型集合文件优先命名为 `types.ts`；与单个接口同名的类型文件可按接口域命名。
- 文件名、变量名和路由文件名必须拼写正确；不得保留仅靠团队记忆识别的拼写错误。
- 静态资源按使用场景分目录，不把所有图片堆在 `assets/` 根目录。

### 3.2 组件命名

- Vue 组件在脚本中使用 PascalCase，例如 `PreviewDialog`、`BarChart`。
- 模板中的局部组件应统一使用 PascalCase；全局组件可以使用 kebab-case。
- 自研全局组件必须携带项目前缀，避免与 HTML、UI 库和第三方组件冲突：

```text
<project-prefix>-button
<project-prefix>-dialog
<project-prefix>-pagination
```

- 组件名应表达“是什么”，避免表达临时位置，例如使用 `ApprovalDialog`，不使用 `RightDialog2`。
- 布局组件、页面组件、弹窗组件和图表组件应使用清楚的职责后缀：`Layout`、`Page`、`Dialog`、`Drawer`、`Chart`。

### 3.3 变量和函数命名

延续“语义名 + 类型/用途后缀”的习惯：

| 对象 | 推荐命名 | 示例 |
| --- | --- | --- |
| 模板引用 | `xxxRef` | `dialogRef`、`canvasRef` |
| 布尔状态 | `isXxx`、`hasXxx`、`xxxVisible` | `isLoading`、`hasPermission`、`dialogVisible` |
| 列表 | `xxxList` | `approvalUserList` |
| 表单 | `xxxForm`、`formData` | `loginForm`、`formData` |
| 筛选条件 | `screen`、`filters` | `filters.status` |
| 事件处理函数 | `handleXxx` | `handleChange`、`handleRemove` |
| 明确的业务动作 | 动词开头 | `openDialog`、`resetForm`、`loadMenuList` |
| 组件暴露方法 | 动词 + 对象 | `openDialog`、`closeDialog`、`destroyPdf` |

历史代码中常见的 `xxxFunc` 后缀可以继续识别，但新代码应优先使用更明确的动词；当函数本身已经清楚表达动作时，不必重复添加 `Func`。

不得使用 `data1`、`result2`、`infoTemp`、`aaa` 等无法体现领域含义的名称。演示数据也应使用能说明结构的名字。

### 3.4 CSS 命名

- 组件根节点类名使用 `<组件语义>-container`，例如 `login-container`。
- 子元素可以沿用简洁的 lowerCamelCase，例如 `loginBox`、`menuItemBox`、`submitBox`。
- 状态类应表达状态，例如 `is-active`、`disabled`、`progressOut`。
- 公共类必须有明确作用域或前缀；页面私有类依赖 `scoped` 隔离。
- 不使用仅描述颜色或位置的业务类名，例如 `redBox`、`left2`，除非它就是稳定的设计语义。

## 4. Vue 单文件组件结构

### 4.1 固定区块顺序

Vue 文件统一按以下顺序组织：

```vue
<template>
    <!-- 结构 -->
</template>

<script setup lang="ts">
// 逻辑
</script>

<style scoped lang="scss">
/* 样式 */
</style>
```

- 必须使用 Vue 3 Composition API 和 `<script setup lang="ts">`。
- 组件样式默认使用 `scoped`；需要嵌套能力时使用 SCSS。
- 仅包含真正全局规则的文件才允许不使用 `scoped`。
- 空的 `<script>` 或 `<style>` 区块应删除。

### 4.2 脚本内部顺序

组件脚本应按稳定顺序排列，降低阅读时的跳转成本：

1. 第三方依赖导入。
2. 项目内组件、Store、API、工具和资源导入。
3. 类型与接口声明。
4. `defineProps`、`defineEmits`、`defineExpose` 所需声明。
5. Router、Store 等组合式实例。
6. 响应式状态和模板引用。
7. 计算属性。
8. 业务函数和事件处理函数。
9. `watch` / `watchEffect`。
10. 生命周期。
11. `defineExpose`。

同一职责的状态和函数应就近放置，并用简短注释形成逻辑区块。注释用于说明“为什么”或业务规则，不复述代码表面含义。

### 4.3 模板规则

- 模板只负责声明结构和绑定，不承载复杂业务计算。
- 复杂条件应提取为计算属性或语义函数，避免连续堆叠多个 `v-if`。
- `v-for` 的 `key` 必须优先使用稳定业务标识；只有静态、不会重排的演示列表才可使用索引。
- 同一元素上的属性按以下顺序书写：结构指令、引用/标识、双向绑定、普通 Props、事件、样式与类。
- 多行组件标签每行承载一组易读属性，结束括号与内容保持清晰。
- 自闭合用于无子节点的组件；有插槽或内容时使用完整闭合标签。
- 不在模板内拼接大段 HTML。若必须使用 `v-html`，内容必须经过可信来源确认或消毒处理。
- 避免模板内联样式；一次性动态值使用 `:style`，稳定样式放入 scoped SCSS。

## 5. 组件拆分习惯

### 5.1 页面只做编排

页面组件负责：

- 组织查询区、操作区、表格区和弹窗区；
- 持有页面级筛选、分页和列表状态；
- 调用业务 API；
- 决定不同子组件何时出现；
- 协调多个子组件之间的数据流。

页面组件不应长期承载：

- 可独立维护的复杂弹窗；
- 具有自身生命周期的图表、编辑器等；
- 大段重复表单；
- 可复用的分页、按钮和列表框架；
- 与页面渲染无关的纯工具逻辑。

### 5.2 何时拆组件

满足以下任一条件时应当拆分：

- 该区域有独立的 Props、事件或内部状态；
- 该区域可以被单独命名并清楚描述职责；
- 该区域具有独立生命周期或资源清理逻辑；
- 同一结构出现两次以上；
- 页面文件超过约 250 行，并且已出现明显职责分组；
- 模板层级过深，主页面结构已难以一屏理解。

不得只为了减少行数而拆出没有边界、仍强依赖父组件内部变量的组件。

### 5.3 弹窗组件模式

复杂弹窗应作为独立组件，并遵循统一控制方式：

```ts
interface DialogPayload {
    id: string;
}

const dialogVisible = ref(false);

const openDialog = (payload?: DialogPayload) => {
    resetForm();
    if (payload) fillForm(payload);
    dialogVisible.value = true;
};

const closeDialog = () => {
    resetForm();
    dialogVisible.value = false;
};

defineExpose({ openDialog, closeDialog });
```

- 父组件通过 `ref` 调用 `openDialog`，适合命令式弹窗场景。
- 子组件通过 Emits 上报确认、取消和成功事件。
- 打开前或关闭后必须明确重置表单，防止复用时残留状态。
- 异步确认期间应提供 loading 和防重复提交状态。
- `defineExpose` 只暴露父组件确实需要的最小方法集合。

简单弹窗也可以使用 `v-model:visible`，但同一类弹窗应保持一种控制模式。

### 5.4 通用框架组件模式

表格框架、弹窗、分页等通用组件应组合已有 UI 库，不重复实现底层能力。推荐边界如下：

- Props：接收外部状态和配置；
- `v-model`：同步分页、可见性等可控状态；
- Emits：上报搜索、重置、翻页、确认等动作；
- Slots：让页面注入筛选项、按钮、主体和 Footer；
- CSS 变量：接收主题，不在组件内复制整套颜色。

通用组件不得读取具体业务 Store、具体路由或具体接口。

## 6. Props、Emits、Slots 与 Expose

### 6.1 Props

- 必须定义 TypeScript 接口，不使用无类型的 `defineProps()`。
- 可选 Props 使用 `withDefaults` 提供合理默认值。
- 数组和对象默认值必须使用工厂函数。
- Props 只读；需要本地编辑时创建副本，并通过 `watch` 处理外部更新。
- Props 名称表达业务含义，不使用 `data`、`info` 作为万能参数。

```ts
interface Props {
    currentPage: number;
    pageSize: number;
    total: number;
    pageSizes?: number[];
}

const props = withDefaults(defineProps<Props>(), {
    pageSizes: () => [20, 30, 50],
});
```

### 6.2 Emits

- Emits 必须声明事件名和参数类型。
- 事件名表达已发生的动作或用户意图，例如 `confirm`、`close`、`changePage`。
- 双向绑定使用 `update:<propName>` 约定。
- 子组件不得通过修改父对象内部字段来替代 Emits。

```ts
const emit = defineEmits<{
    confirm: [id: string];
    close: [];
    changePage: [currentPage: number, pageSize: number];
    'update:currentPage': [value: number];
}>();
```

### 6.3 Slots

- Slot 用于“结构可变、职责稳定”的区域。
- 默认插槽承载主体，命名插槽使用职责名，例如 `screen`、`operation`、`footer`。
- 组件应根据 `$slots` 是否存在决定是否渲染外围容器，避免空白布局。
- Slot 名必须是稳定 API，不使用页面业务名称。

### 6.4 Expose

- Expose 适用于弹窗开关、画布销毁、编辑器聚焦等命令式能力。
- 不暴露内部 `ref`、表单对象或第三方实例，除非父级确有不可替代的需求。
- 暴露方法需要稳定、可预测，并负责维护自身状态完整性。

## 7. TypeScript 约束

### 7.1 类型优先

- 必须保持 `strict` 模式。
- 业务实体、接口参数、组件 Props 和 Emits 必须有明确类型。
- 仅导入类型时使用 `import type`。
- 优先使用 `T[]` 表示简单数组；复杂泛型可使用 `Array<T>`，同一文件保持一致。
- 可空 DOM 引用写为 `ref<HTMLElement | null>(null)`。
- 第三方实例应使用库提供的类型，不用 `any` 绕过。
- 捕获异常默认视为 `unknown`，通过类型守卫读取信息。

### 7.2 禁止滥用 `any`

以下情况不得使用 `any`：

- 用户、表格行、表单、路由 Meta 等稳定业务结构；
- API 响应；
- 上传文件、图表实例和第三方对象已有官方类型时；
- 仅因为 TypeScript 报错而临时跳过检查。

当外部数据确实未知时使用 `unknown`，在边界处校验后转换。若因第三方声明缺失必须使用 `any`，应将它限制在适配器内部并添加原因注释。

### 7.3 类型放置

- 只在单个组件使用的类型放在该组件脚本顶部。
- 同一功能多个文件共用的类型放到功能目录的 `types.ts`。
- API 契约类型放到 `api/types/`。
- 全局通用类型才进入全局声明文件；不得把业务类型全部塞入 `global.d.ts`。

### 7.4 返回值与常量

- 导出的工具函数和公共方法应显式标注返回类型。
- 状态码、角色、主题名、存储键等重复字符串应抽为常量或字面量联合类型。
- 比较默认使用严格等号 `===` / `!==`。
- 魔法数字应命名；视觉尺寸只在局部出现一次时可以保留在样式中。

## 8. 格式与代码风格

### 8.1 缩进和换行

- Vue 模板、Vue 脚本和 SCSS 使用 4 个空格缩进。
- 独立 TypeScript 与工程配置可以使用 2 个空格，但同一文件不得混用。
- 不使用 Tab。
- 过长的参数、对象、数组和组件属性按语义换行，避免横向滚动。
- 逻辑段之间保留一个空行，不连续堆叠多个空行。
- 文件末尾保留一个换行。

### 8.2 引号和分号

既有代码在 Vue 文件中更偏向单引号，在独立 TypeScript 与配置中更偏向双引号。通用约束如下：

- 同一文件必须统一引号风格。
- 若项目已接入 Prettier，以 Prettier 配置为准。
- 无格式化配置时，Vue 脚本使用单引号，独立 TypeScript 使用双引号。
- TypeScript 语句统一使用分号，避免同一文件时有时无。
- 模板属性使用双引号。

### 8.3 导入

- `src` 内跨模块导入使用 `@/` 别名。
- 同目录或紧邻私有文件使用相对路径。
- 不混用带扩展名和不带扩展名的导入；项目源码默认省略 `.ts` 和 `.vue`。
- 导入按“第三方、项目绝对路径、本地相对路径、资源/样式”分组。
- 删除未使用导入，不依赖编译器忽略。
- 自动生成的 `auto-imports.d.ts`、`components.d.ts` 不手工编辑。

### 8.4 注释

- 注释使用完整、简洁的中文或团队约定语言。
- 行注释与代码之间留一个空格：`// 权限判断`。
- 公共工具、复杂参数和非显然返回值使用 JSDoc。
- TODO 必须说明待办原因，最好关联任务编号。
- 删除失效、乱码和与实现不一致的注释。

## 9. 响应式状态与数据流

### 9.1 `ref`、`reactive` 和普通常量

- 基本值、可整体替换的对象、模板引用优先使用 `ref`。
- 多字段对象且不需要整体替换时可以使用 `reactive`。
- 不参与响应式渲染的数据使用普通 `const` / `let`。
- 大型第三方实例使用 `markRaw` 或普通变量，避免无意义的深层代理。
- 不为只读派生值复制状态，使用 `computed`。

### 9.2 状态归属

按以下顺序决定状态放置位置：

1. 只服务当前组件：组件内状态。
2. 父子组件共享：提升到最近公共父组件，通过 Props/Emits 传递。
3. 同一功能多个远距离组件共享：功能级组合式函数或功能 Store。
4. 跨页面、跨布局共享：Pinia 全局 Store。
5. 需要刷新后保留：在 Store/工具层统一同步到浏览器存储。

不得把所有状态都放入全局 Store，也不得让多个组件直接读写同一个浏览器存储键来实现隐式通信。

### 9.3 Watch 与生命周期

- `watch` 只用于副作用和外部同步，派生数据使用 `computed`。
- Watch 源应尽量精确，避免深度监听整个大对象。
- `onMounted` 用于依赖真实 DOM 的初始化。
- 定时器、事件监听器、图表、PDF、Object URL 等资源必须在卸载或关闭时清理。
- 异步渲染应处理竞态，必要时取消上一次任务或忽略过期结果。

## 10. 路由与菜单

- 路由实例和业务路由表分离，便于维护基础路由与功能路由。
- 页面组件默认懒加载；布局等首屏骨架可直接导入。
- 路由 `name` 使用唯一的 PascalCase。
- 子路由 `path` 使用相对路径，不以 `/` 开头。
- 页面标题、图标、是否显示、角色权限等放在 `meta` 中。
- 菜单由路由 Meta 派生，避免维护第二份重复菜单配置。
- 菜单可见性不等于访问权限；路由守卫必须独立校验权限。
- 未登录、无权限和未知路由应有明确落点。
- 路由守卫的用户类型和 Meta 类型必须显式扩展，不依赖 `any`。

推荐 Meta：

```ts
interface AppRouteMeta {
    title: string;
    icon?: string;
    showLink?: boolean;
    roles?: string[];
}
```

## 11. API 与请求层

### 11.1 分层职责

- `service/` 只负责通用 HTTP 能力：基础地址、超时、请求头、统一响应、错误、鉴权和重试。
- `api/` 按业务域导出具体请求函数。
- `api/types/` 描述请求和响应结构。
- 页面不直接创建 Axios 实例，也不重复拼接鉴权头。
- Mock 与真实 API 应保持相同响应契约，切换时不改页面调用方式。

```ts
export interface LoginParams {
    username: string;
    password: string;
}

export interface LoginResult {
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
}

export const login = (params: LoginParams): Promise<LoginResult> => {
    return http.post('/login', params);
};
```

### 11.2 请求行为

- Base URL 来自环境变量，不写死在业务代码中。
- 请求超时必须统一配置。
- Token 由请求拦截器附加。
- 响应层统一决定返回完整响应还是业务数据，项目内不得混用。
- 401 刷新 Token 时只允许一个刷新请求，其余请求进入队列等待。
- 刷新失败必须清空凭据、拒绝队列并回到登录页。
- 无论成功失败，全局进度和 loading 都必须正确结束。
- 错误消息避免在 service 和页面重复弹出；应明确由哪一层负责用户提示。
- 重试必须有次数上限和幂等性判断。

### 11.3 浏览器存储

- Token、用户信息、主题等存储操作统一封装在工具或 Store 中。
- 存储键使用常量并带项目命名空间。
- 读取 JSON 必须 `try/catch`，解析失败时清理无效数据。
- Base64 不是加密，不得用它保存真实明文密码。
- 敏感信息优先使用更安全的服务端会话或 HttpOnly Cookie 方案。

## 12. Store 约束

- Store 名称带项目或业务域前缀，避免调试工具中冲突。
- State 必须有明确接口。
- Action 使用动词命名，例如 `setUser`、`startProgress`、`finishProgress`。
- Store 负责状态及状态迁移，不承载 DOM 操作。
- 持久化读取逻辑集中在初始化 Action 中，不散落在页面。
- 异步 Action 必须处理失败并恢复 loading 状态。
- 定时器需要保存句柄并在重复启动前清理，避免并发计时器修改同一状态。

## 13. 样式与主题

### 13.1 全局样式

全局样式只包含：

- Reset 和 `box-sizing`；
- 字体声明；
- Body 基础颜色；
- 全局滚动条；
- 设计系统明确要求的 UI 库覆盖；
- 主题变量。

页面细节不得写入全局样式。

### 13.2 Scoped SCSS

- 组件样式默认 `scoped lang="scss"`。
- SCSS 嵌套应与 DOM 层级一致，但不超过约 3 层；更深时拆类名。
- UI 库内部样式使用 `:deep()`，并限制在当前组件根类下。
- 伪类和状态写在对应元素内部，例如 `&:hover`。
- 布局优先使用 Flex 和 Grid，使用 `gap` 表达元素间距。
- 动画命名表达用途，进入、离开状态成组定义。

### 13.3 主题变量

- 可主题化的颜色必须使用 CSS 变量，不散落硬编码。
- 变量按“全局、页面骨架、导航、组件”分组。
- 变量名表达用途，不表达某个主题下的实际颜色：

```css
:root {
    --font-color: #151515;
    --border-color: #e4e4e4;
    --menu-background-color: #18171c;
    --button-primary-background-color: #0065ca;
}

.theme-green {
    --menu-background-color: #fcfcfc;
    --button-primary-background-color: #32af81;
}
```

- 新主题只覆盖变量，不复制组件样式。
- 组件专属变量可带组件名，通用令牌不带项目业务名。
- 主题类挂在 `html` 或应用根节点，并持久化用户选择。

### 13.4 内联样式

- 固定视觉样式必须进入 CSS 类。
- 仅由运行时数据决定的值使用 `:style`，例如动态主题预览色。
- 不在模板中重复长 `style` 字符串。
- 状态颜色较多时使用状态映射生成类名，避免为每个状态复制 `v-if + style`。

## 14. 第三方能力封装

图表、PDF、上传、富文本等第三方能力应包裹为边界清楚的组件：

- 按需导入模块，避免全量注册。
- DOM 实例在 `onMounted` 后创建。
- Props 更新时只更新必要配置，不无条件重复创建实例。
- 在 `onBeforeUnmount` 中调用 `dispose` / `destroy`。
- 监听容器尺寸变化并调用 `resize`。
- 初始化、加载、空数据和失败状态必须在 UI 上可辨认。
- 第三方类型通过适配层收敛，页面不直接依赖复杂内部类型。
- 组件对外暴露业务动作，不暴露第三方实例本身。

## 15. 表单、列表与交互

### 15.1 表单

- 表单模型使用明确接口和初始值工厂。
- 重置时恢复完整初始对象，不逐字段遗漏。
- 提交前使用组件库校验规则。
- 关闭弹窗时清理校验状态、上传列表和临时文件。
- 选项列表的 `label` 与 `value` 类型保持稳定。
- 用户操作成功或失败应提供统一消息反馈。

### 15.2 查询与分页

- 查询条件、页码、每页数量集中在一个筛选对象中。
- 点击搜索时页码重置为 1。
- 点击重置时清空筛选项并恢复默认分页。
- 分页组件通过 `v-model` 同步页码和每页数量，并发出统一 `changePage` 事件。
- 总条数来自接口结果，不由当前页数组长度推断。
- 发起新请求时应处理旧请求竞态。

### 15.3 状态展示

- 状态码到文案、颜色和权限的映射集中定义。
- 模板通过一个映射结果渲染，不为每个状态复制一段结构。
- 删除等不可逆操作必须二次确认。
- 禁用状态同时阻止事件并提供视觉反馈。
- 可点击的非原生按钮元素必须补充键盘操作和语义；优先直接使用 `<button>` 或组件库按钮。

## 16. 工具函数

- 工具函数应单一职责、无组件依赖、尽量纯函数化。
- 文件名与主导出函数同义。
- 输入、输出和异常行为必须明确。
- 默认参数只用于真正稳定的默认行为。
- DOM 型工具必须清理临时节点、事件和 Object URL。
- 时间、下载、防抖、Token 等通用能力分别放置，不创建万能 `utils.ts`。
- 公共工具应配单元测试，至少覆盖边界输入与失败路径。

## 17. 错误处理、日志与用户反馈

- 不吞掉异常；无法处理时继续向上抛出。
- 页面只展示用户可理解的信息，技术细节进入开发日志或监控。
- `console.log` 不得进入生产代码；`console.warn/error` 仅用于确有诊断价值的异常。
- 同一错误只能由一个层级负责弹窗，避免多次提示。
- 异步函数使用 `try/catch/finally` 保证 loading、进度条和禁用状态复原。
- 空数据、网络失败、无权限和登录过期应有不同反馈。
- 错误对象使用 `unknown`，经 Axios 或自定义守卫缩小类型。

## 18. 安全与权限

- 前端菜单隐藏只改善体验，不能代替后端鉴权。
- 路由守卫执行页面级权限校验。
- 按钮级权限通过统一指令、组件或权限函数判断，不在各页面复制角色字符串。
- 不信任 URL 参数、浏览器存储、后端富文本和上传文件名。
- `v-html` 内容必须消毒。
- 下载文件名应过滤路径字符。
- 上传必须同时限制扩展名、MIME、大小和数量；服务端再次校验。
- 不在源码、环境示例、Mock 和日志中提交真实密钥或 Token。

## 19. 工程质量与提交前检查

项目必须至少提供以下脚本：

```json
{
    "scripts": {
        "dev": "vite",
        "typecheck": "vue-tsc --noEmit",
        "lint": "eslint .",
        "build": "vite build",
        "preview": "vite preview"
    }
}
```

建议补充单元测试和端到端测试。提交前必须：

1. 运行类型检查。
2. 运行 Lint 和格式检查。
3. 运行与改动相关的测试。
4. 完成生产构建。
5. 检查控制台无新增错误与无用日志。
6. 检查新增交互的 loading、空态、失败态和权限态。
7. 检查组件卸载后无事件、定时器和第三方实例泄漏。

## 20. 明确禁止固化的偶发写法

以下写法可能出现在早期代码或演示代码中，但不属于应保留的项目习惯：

- 大量使用 `any` 代替业务类型。
- 同一文件混用单双引号、分号和缩进。
- 导入路径有时带扩展名、有时不带。
- 用索引作为所有 `v-for` 的 Key。
- 在模板中堆叠大段内联样式。
- 用多个 `v-if` 复制同一状态卡片结构。
- 使用 `==` 进行宽松比较。
- 在页面直接读写 Token 或拼接鉴权头。
- 用 Base64 当作密码加密。
- 未清理图表、PDF、定时器或事件监听器。
- 声明 Emits、Props 或变量后不使用。
- 注释乱码、拼写错误、错误的事件名和名实不符的 Props。
- 组件声明了事件但永不发出，或父组件监听不存在的事件。
- 将演示数据、接口契约和页面渲染永久混在一个大型组件中。

## 21. 新增功能执行模板

新增一个常规列表功能时，按以下顺序实施：

1. 在 `api/types/` 定义请求、响应和实体类型。
2. 在 `api/` 添加领域接口函数。
3. 在 `views/<feature>/index.vue` 创建页面入口。
4. 将复杂弹窗放入 `views/<feature>/components/`。
5. 页面维护筛选、分页、表格数据和弹窗引用。
6. 复用全局表格框架、分页、按钮和弹窗组件。
7. 使用状态映射处理文案、颜色和可操作项。
8. 仅当出现真实跨页面复用时，将组件提升到 `components/range/` 或 `components/global/`。
9. 补齐 loading、空态、错误态、权限态和销毁逻辑。
10. 通过类型检查、Lint、测试和构建。

## 22. 约束维护方式

- 约束应随稳定实践演进，不因一次特殊需求立即修改。
- 同一例外出现三次以上时，应评估是否抽象为新规则或公共能力。
- 修改约束时应同时更新脚手架、Lint、格式化器或代码模板，尽量让规则可自动执行。
- 无法自动执行的架构规则通过 Code Review 清单维护。
- 历史代码采用“修改即收敛”策略：触及某文件时修正相关不一致，不要求一次性重写整个仓库。